unit MDescription1;

interface

uses
  {$IF DECLARED(FireMonkeyVersion)}
    FMX.Dialogs,
  {$ELSE}
    Vcl.Dialogs,
  {$ENDIF}
  M4D,
  UMigrations;

type
  TMDescription1 = class(IMigrations)
  public
    constructor Create; reintroduce;
    procedure Up;
    procedure Down;
  end;

implementation

uses
  System.SysUtils;

{ TMDescription1 }

constructor TMDescription1.Create;
begin
  Self.Version := '1.00';
  Self.SeqVersion := 1;
  Self.DateTime := StrToDateTime('01/09/2017 07:13:00');
end;

procedure TMDescription1.Down;
begin
  ShowMessage('Executing up!');
end;

procedure TMDescription1.Up;
begin
  ShowMessage('Executing up!');
end;

initialization
  RegisterMigration(TMDescription1);


end.
